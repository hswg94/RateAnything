<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand fw-bold fst-italic" href="/">RateAnything</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link <%= currentUrl === '/' ? 'active' : '' %>" href="/">Home</a>
                <a class="nav-link <%= currentUrl === '/items' ? 'active' : '' %>" href="/items">View</a>
                <a class="nav-link <%= currentUrl === '/items/new' ? 'active' : '' %>" href="/items/new">Submit</a>
                <% if (!currentUser) { %>
                    <a class="nav-link <%= currentUrl === '/login' ? 'active' : '' %>" href="/login">Login</a>
                    <a class="nav-link <%= currentUrl === '/register' ? 'active' : '' %>" href="/register">Register</a>
                <% } else {%>
                    <a class="nav-link" href="/logout">Logout</a>
                <% } %>
            </div>
        </div>
    </div>
</nav>
